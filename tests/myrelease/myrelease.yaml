replicaCount: 1

image:
  repository: docker/whalesay
  tag: "v1.0.0"
  pullPolicy: IfNotPresent

deployment:
  enabled: true
  revisionHistoryLimit: 10
  pod:
    annotations: {}
    labels: {}
    command:
      - "/bin/bash"
      - "-c"
      - "--"
    args:
      - "while true; do cowsay -f tux 'I love Gitops from Production'; sleep 30; done;"
